services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: llmcouncil
      POSTGRES_PASSWORD: llmcouncil
      POSTGRES_DB: llm_council
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
